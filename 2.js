var resultadosubtracao = divide(30, 2);
divlocal = document.getElementById("resultado_subtracao");
divlocal.innerHTML += resultadosubtracao;

function divide(oi2, oi3){
    var resultado = 0;

    resultado = oi2 - oi3
    
    return resultado
}